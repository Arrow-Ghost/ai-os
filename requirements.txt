# The framework core runs on the standard library alone.
# `git clone && python -m servant tools` works with none of this installed.

# --- needed to edit policy.yaml (otherwise defaults are used) ---
PyYAML>=6.0

# --- needed for brain.provider: groq ---
openai>=1.40.0

# --- calendar.* (already pulled in transitively by browser-use, listed here
#     so calendar works even if browser-use is skipped) ---
google-auth-oauthlib>=1.2
google-api-python-client>=2.100

# --- Track B features ---
ddgs>=9.0                # web.search -- keyless DuckDuckGo, no quota
playwright>=1.47         # browser.browse (uses your installed Chrome:
                         #   channel="chrome", so no browser download needed)
apscheduler>=3.10        # servant watch -- interval scheduling for monitors
Pillow>=10.0             # screen.* -- downscales screenshots to save tokens

# --- tests ---
pytest>=8.0

# --- Track A: features/sys_tools.py (sys.processes, sys.disk, sys.battery) ---
psutil>=6.0

# --- system packages, not pip (Ubuntu/KDE) -----------------------------
#   libnotify-bin     notify.send
#   wl-clipboard      clipboard.*  (xclip on X11)
#   kde-spectacle     screen.*     (grim on wlroots)
#   pipewire-utils    speech.record  (or pulseaudio-utils / alsa-utils)

# --- add per feature, not up front -------------------------------------
# Pin versions when you add these; several move fast.
#   browser-use>=0.13.10    browser control. Drives Chrome over CDP -- it does
#                           NOT need Playwright, and it works on Wayland.
#   mss>=9.0                screenshots on X11 ONLY -- does not work on Wayland
#   watchdog>=4.0           filesystem events
#   faster-whisper>=1.0     local speech to text, runs on your GPU
